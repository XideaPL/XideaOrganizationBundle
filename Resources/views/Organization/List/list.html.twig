{% extends xidea_template('organization_main') %}
{% block bundle_title %}
    <h1 class="page-header">{{ 'organization_list.title'|trans }}</h1>
{% endblock %}
{% block bundle_main %}
    <div class="box box-default organization-box">
        <div class="box-header">
            <h3 class="box-title">{{ 'organization_list.title'|trans }}</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <div class="organization-list table-responsive">
                <table class="table table-striped organization-table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>{{ xidea_base_sorting(models.sorting, 'organization.name'|trans, 'o.name') }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for model in models %}
                            <tr>
                                <td>{{ model.id }}</td>
                                <td>{{ model.name }}</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>#</th>
                            <th>{{ 'organization.name'|trans }}</th>
                        </tr>
                    </tfoot>
                </table>
                {{ xidea_base_pagination(models) }}
            </div>
            <!-- /.table-responsive -->
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
{% endblock %}
